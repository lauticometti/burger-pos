rules_version = '3';
service cloud.firestore {
  match /databases/{database}/documents {
    // POS sin autenticación - app interna del local
    match /orders/{document=**} {
      allow read, write: if true;
    }
  }
}
